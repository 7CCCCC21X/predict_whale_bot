services:
  predict-whale-bot:
    image: python:3.12-slim
    container_name: predict-whale-bot
    working_dir: /app
    volumes:
      - ./:/app
    env_file:
      - .env
    command: sh -c "pip install --no-cache-dir -r requirements.txt && python predict_whale_bot.py"
    restart: unless-stopped
